# Pages légales autonomes (HTML pur, lisibles par les robots — ex. validation Google OAuth).
# Les fichiers .html sont servis directement ; ces règles donnent en plus des URLs propres.
/privacy            /confidentialite.html   200
/confidentialite    /confidentialite.html   200
/cgu                /cgu.html               200
/conditions         /cgu.html               200

# L'app (FINEA.html) sur toutes les autres routes. 200 = rewrite (l'URL reste propre).
/*                  /FINEA.html             200
